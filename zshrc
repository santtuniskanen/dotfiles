# Starship
eval "$(starship init zsh)"

# zoxide
eval "$(zoxide init zsh)"

# Aliases
alias ls="exa --icons -l"
alias tmuxcheatsheet="glow ~/.dotfiles/.tmux-windows-panes-cheatsheet.md"

export PATH="$HOME/.local/bin:$PATH"
